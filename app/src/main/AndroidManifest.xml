<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
      IMPORTANT: Added maxSdkVersion for modern Android compatibility.
      This is the correct way to declare storage permissions now.
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Camera Features -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/zilhacoffeshop"
        android:label="@string/app_name"
        android:roundIcon="@drawable/zilhacoffeshop"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">  <!-- ⭐ THIS IS THE FIX ⭐ -->

        <!-- Activities -->
        <activity
            android:name=".Activity.MainActivity2"
            android:exported="false" />
        <activity
            android:name=".Activity.CartActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.FavItemActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.OrdersActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.RecommendationActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.ListActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.ItemDetailActivity"
            android:exported="false" />

        <!-- Splash Screen (Launcher) -->
        <activity
            android:name=".Activity.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- No Internet Activity -->
        <activity
            android:name=".Activity.NoInternetActivity"
            android:exported="false" />

    </application>

</manifest>
